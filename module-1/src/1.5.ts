{
  const user: {
    roll: 'Will be a user';
    id: number;
    name: string;
    age: number;
    address?: string;
  } = {
    roll: `Will be a user`,
    id: 1,
    name: `special user 1`,
    age: 35,
  };
}
