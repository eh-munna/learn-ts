{
  // defining the string data type
  let text: string = `This is a demo text`;

  const num: number = 10;

  const isBoolean: boolean = true;

  const arr: string[] = [`a`, `b`, `c`, `d`, `e`];

  const arrTuple: [string, number, boolean] = ['Mr XYZ', 56, true];
}
